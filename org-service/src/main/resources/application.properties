spring.application.name=org-service
spring.config.import=optional:file:.env[.properties]

# Server Configuration
server.port=${ORG_APP_PORT:8081}

# Timezone
spring.jackson.time-zone=UTC

# Database Configuration
spring.jpa.hibernate.ddl-auto=none
spring.datasource.url=jdbc:postgresql://localhost:${POSTGRES_PORT}/${ORG_POSTGRES_DB}?TimeZone=UTC
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Properties
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.show-sql=${SHOW_SQL:false}

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1

jwt.secret=${JWT_SECRET}

grade-service.url=http://localhost:${GRADE_APP_PORT:8082}